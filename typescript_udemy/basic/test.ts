const book: [string, number ,boolean] = ["hello", 1500,false];

enum CoffeeSize {
    SHOT = "SHORT",
    TALL = "TALL"
}

const coffee = {
    hot: true,
    size: CoffeeSize.TALL
}

// coffee size can only be used in enum setting

